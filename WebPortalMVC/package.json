{
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.63.6",
    "sass-compiler": "sass --watch scss:css --style expanded",
    "start": "live-server"
  },
  "scripts": {
    "sass:watch": "sass --watch scss:css --style expanded",
    "start": "live-server"
  }
}
